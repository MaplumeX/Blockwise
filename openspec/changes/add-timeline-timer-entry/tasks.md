## 1. Implementation
- [ ] 1.1 在 Timeline UIState 中暴露“是否可展示计时入口”的条件（仅 Timeline 且 selectedDate=Today）。
- [ ] 1.2 在 Timeline 列表中插入计时入口按钮（列表内，最新条目下方；按时间正序=插入在 items 之后），并实现无障碍语义与 48dp 触摸目标校验。
- [ ] 1.2.1 Today 中隐藏 Timeline quick create FAB（右下角 "+"），避免双入口。
- [ ] 1.3 复用现有活动类型选择器（Dialog/BottomSheet 之一），实现“开始计时”选择流程：确认后开始；取消不产生条目。
- [ ] 1.4 引入/复用计时状态（单计时器约束），并将状态注入 Timeline：
- [ ] 1.4.1 计时中时按钮文案切换为“完成”，禁止再次进入开始流程。
- [ ] 1.4.2 Timeline 列表渲染“计时中条目”（活动类型 + 起始时间 + 结束时间=进行中，随时间更新）。
- [ ] 1.5 完成计时：停止并创建 TimeEntry（秒级精度），保证 end > start（<= 时补 +1s）。
- [ ] 1.6 跨日拆分：若 start/end 跨越 24:00:00，按自然日拆分为两条 TimeEntry 写入。
- [ ] 1.7 单元测试：
- [ ] 1.7.1 end <= start 的 +1s 规则。
- [ ] 1.7.2 跨日拆分规则（23:59:59 -> 00:00:10）。
- [ ] 1.8 UI 测试/集成验证（可选）：Today Timeline 下按钮可见性、取消选择不产生条目、完成后列表刷新。
- [ ] 1.9 验证：`./gradlew test`（至少 JVM tests）+ detekt/ktlint 通过（如项目 CI 约束）。
