package com.maplume.blockwise.core.common.constants

/**
 * Application-wide constants.
 */
object AppConstants {
    /**
     * Database name.
     */
    const val DATABASE_NAME = "blockwise_database"

    /**
     * Database version.
     */
    const val DATABASE_VERSION = 1

    /**
     * Default page size for pagination.
     */
    const val DEFAULT_PAGE_SIZE = 20

    /**
     * Maximum number of recent activities to display.
     */
    const val MAX_RECENT_ACTIVITIES = 10

    /**
     * Minutes in an hour.
     */
    const val MINUTES_PER_HOUR = 60

    /**
     * Hours in a day.
     */
    const val HOURS_PER_DAY = 24

    /**
     * Minutes in a day.
     */
    const val MINUTES_PER_DAY = MINUTES_PER_HOUR * HOURS_PER_DAY
}
